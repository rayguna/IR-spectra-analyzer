In order for the program to run properly, make sure: 

A. To have the following files in the same directory:
1. flowchart.png (image)
2. NIST_chemicals_list.csv (a three-column table of: chemical name, chemical formula, and CAS no.).
3. NIST_periodic_table.txt

B. To have the following python packages installed
from IPython.display import display, Image, SVG, Math, YouTubeVideo
import collections
import pandas as pd
import re 
import requests
from bs4 import BeautifulSoup 
from peakutils.peak import indexes as index_utils 
import urllib.request
import requests

-Program was tested successfully on anaconda 2 using python 3.5 and 3.6 compilers.
